import { Writable } from 'svelte/store';
import { ViewToggleSize } from './view-toggle-size.enum';
export interface ViewToggleStoreProps {
    selectedid: string;
    size?: ViewToggleSize;
}
export interface CustomViewToggleStore extends Writable<ViewToggleStoreProps> {
    updateSelectedItem: (newSelectedItem: string) => void;
}
