import { PopoverTriggerChipVariant } from '../../types';
import { IconColor } from '../../../Icon/types';
import { Readable } from 'svelte/store';
import { TextColor } from '../../../../index-component-types';
export interface PopoverTriggerChipState {
    hostElement?: HTMLElement;
    variant?: PopoverTriggerChipVariant;
    hovered?: boolean;
}
export interface PopoverTriggerChipViewModel {
    iconcolor: IconColor;
    textcolor: TextColor;
}
export interface CustomPopoverTriggerChipStore extends Readable<PopoverTriggerChipViewModel> {
    updateVariant: (variant: PopoverTriggerChipVariant) => void;
    updateHovered: (hovered: boolean) => void;
}
export declare const createPopoverTriggerChipStore: (hostElement: HTMLElement, variant?: PopoverTriggerChipVariant) => CustomPopoverTriggerChipStore;
