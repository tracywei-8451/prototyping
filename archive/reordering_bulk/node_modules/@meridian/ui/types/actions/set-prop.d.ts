import { SvelteAction } from './svelte-action';
interface SetPropActionParams {
    property: string;
    value: unknown;
}
/**
 *
 * This is needed when we have a web component that accepts a string or an array.
 * Svelte is converting the array to a string and won't let us pass as an array.
 */
export declare const setProp: (node: HTMLElement, params: SetPropActionParams) => SvelteAction;
export {};
