import { TransitionConfig } from 'svelte/transition';
import { Tweened } from 'svelte/motion';
import { TextSize } from '../components/Text/types/text-size.enum';
import { TextFontFamily } from '../components/Text/types/text-font-family.enum';
export declare class MdsAnimationUtility {
    static noAnimation(): Tweened<number>;
    /**
     * Get the transition duration based on user preferences.
     * @param duration The desired duration of the transition in milliseconds. Defaults to 200ms.
     * @returns `duration` if user has not set reduced motion on their browser.
     */
    static getTransitionDuration(duration?: number): number;
    static getSvelteSlideTransitionProps(duration?: number): TransitionConfig;
    static getRotateAnimation(initialValue: number): Tweened<number>;
    static getQuintOutAnimation(initialSize: number): Tweened<number>;
    static getExtendedQuintOutAnimation(initialSize: number): Tweened<number>;
    static getColorAnimation(initialColor: string): Tweened<string>;
    static getFlyInAnimationProps(): object;
    static getFlyOutAnimationProps(): object;
    static calculateRenderedTextWidth(textContent: string, fontSize: TextSize, fontFamily: TextFontFamily): number;
    static getElementHeightById(id: string): number;
    /**
     * Used for animations that occur on objects that render immediatlly .
     * @param animation The animation to call if the browser window is available.
     * @returns The animation parameter if the browser window is available, otherwise an animation that does nothing.
     */
    static callAnimationBasedOnWindowState(animation: () => Tweened<any>): Tweened<number>;
}
