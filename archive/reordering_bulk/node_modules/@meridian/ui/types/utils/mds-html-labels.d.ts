import { MdsSafeHtmlString } from '@meridian/core';
/**
 * TODO, we need to find a better way to get slim reset into our web components.
 * We think Svelte 4 might allow us to use "global" on the style tag inside each web component, e.g. <style global>
 * and then we can prepend the slim-reset.scss file using rollup to each component.
 */
export declare const getHtmlLabel: (textOrHtml: string | MdsSafeHtmlString, disableSlimReset?: boolean, customStyling?: string) => string;
