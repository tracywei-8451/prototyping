import { Writable } from 'svelte/store';
export interface VirtualScrollStoreProps {
    scrollTop: number;
    visibleIndexRange: number[];
    itemHeight: number;
    viewportHeight: number;
    bufferItemAmount: number;
}
export interface VirtualScrollStore extends Writable<VirtualScrollStoreProps> {
    handleScroll: (event: Event) => void;
    updateVisibleItemIndicies: () => void;
    shouldItemBeRendered: (index: number, visibleIndicies: number[], itemListLength: number) => boolean;
}
