import { VirtualScrollStore } from './virtual-scroll-store.interface';
export declare const createVirtualScrollStore: (scrollTop: number, visibleIndexRange: number[], itemHeight: number, viewportHeight: number, bufferItemAmount: number) => VirtualScrollStore;
