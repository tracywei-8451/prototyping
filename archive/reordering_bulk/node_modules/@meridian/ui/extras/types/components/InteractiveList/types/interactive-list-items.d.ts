import { IconName } from '../../Icon/types';
export interface MdsInteractiveListItem {
    id: string;
    title: string;
    enabled?: boolean;
    reorderable?: boolean;
    toggleable?: boolean;
    icon?: IconName;
}
