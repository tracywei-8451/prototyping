import { InternalRoute } from '@meridian/core';
import { JSX } from '../../../../stencil-public-runtime';
export declare class PageHeaderSubheaderLinkComponent {
  /**
   * The label of the link.
   */
  linklabel: string;
  /**
   * The displayed text of the link.
   */
  linktext: string;
  /**
   * Destination after clicking the component. This is meant for an external link. Alternatively you can use the routerLink property to navigate to an Angular specific internal route.
   */
  href: string;
  /**
   * Used in conjunction with the href or route attributes. Specifies the target attribute on the anchor tag.
   */
  target: string;
  /**
   * Provide details to route via the framework's router instead of a traditional href on an anchor tag.
   *
   * Use this to keep routing within a single page app instead of letting a whole page load occur.
   */
  route: InternalRoute;
  render(): JSX.Element;
}
