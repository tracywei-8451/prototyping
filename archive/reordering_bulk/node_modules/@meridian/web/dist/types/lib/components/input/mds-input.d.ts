import { IconName, InputMask, InputType, InputSize } from '@meridian/ui/extras';
import { EventEmitter, JSX } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { MdsInputMaskConfig } from './input-mask-config';
export declare class InputComponent {
  /**
   * The value of the input control
   */
  value: string;
  /**
   * The placeholder for the control
   */
  placeholder: string;
  /**
   * Whether or not the input is disabled
   */
  disabled: boolean;
  /**
   * The input's type. If using a mask, this should not be passed as 'text' is required which is the default.
   */
  type: InputType;
  /**
   * An optional icon to display to the left of the input
   */
  icon: IconName | null;
  /**
   * Whether the input should be displayed with an error state
   */
  error: boolean;
  /**
   * Whether the input should be displayed with a warning state
   */
  warning: boolean;
  /**
   * @deprecated DEPRECATED: Whether to put focus on this element on page load. For a11y reasons, this shouldn't be used.
   */
  autofocus: boolean;
  /**
   * Whether to make this field readonly
   */
  readonly: boolean;
  /**
   * When true, renders the value as read-only text (no native input element).
   *
   * This is meant for cases where the value is part of a form-like layout but
   * is not editable in the current workflow.
   */
  displayonly: boolean;
  /**
   * The max number of characters allowed to be entered. Note: this will not be applied when also using property mask. To restrict length in addition to a mask, use one of the existing masks or a regex mask and supply your own regex.
   */
  maxlength: number;
  /**
   * The type of mask to apply. When using a mask, type will be forced to 'text'.
   */
  mask: InputMask;
  /**
   * Configuration options to use with the mask property.
   *
   * Use MdsInputMaskConfig by passing an object adhering to the interface.
   */
  maskconfig: MdsInputMaskConfig;
  /**
   * Strip the input value of all special characters (except decimals) and emit as a number. Impacts mdsChange, mdsBlur, mdsFocus. Does not impact the value property.
   */
  emitasnumber: boolean;
  /**
   * Property is set on the input and can be used to turn autocomplete/autofill off.
   */
  autocomplete: string;
  /**
   * Property to hide the numeric arrows on a number input.
   */
  hidenumericarrows: boolean;
  /**
   * Size of the text input
   */
  size: InputSize;
  /**
   * Event emitted when the value of the input control changes
   */
  mdsChange: EventEmitter<string | number>;
  /**
   * Event emitted when the input control loses focus
   */
  mdsBlur: EventEmitter<string | number>;
  /**
   * Event emitted when the input control gains focus
   */
  mdsFocus: EventEmitter<string | number>;
  /**
   * Method is called to focus onto the input
   */
  focusInput(options?: FocusOptions): Promise<void>;
  /**
   * Method is called to blur (or focus off of) the input
   */
  blurInput(): Promise<void>;
  /**
   * Method to set focus on input
   */
  private setFocusOnInput;
  /**
   * Method to clear input
   */
  private clearInput;
  hostElement: HTMLStencilElement;
  private passwordIcon;
  private inputMask;
  private valueChangedInternally;
  private MASK_CONFIG;
  private INPUT_ID;
  componentDidLoad(): void;
  valueChanged(): void;
  maskChanged(): void;
  setMaskValue(): void;
  getInput(): HTMLInputElement | null;
  hasMask(): boolean;
  setupMask(): void;
  togglePasswordLegibility(): void;
  getMaskConfig(): any;
  getValueFromEvent(event: any): string | number;
  getReturnValue(): string | number;
  onBlur(event: any): void;
  onInput(event: any): void;
  onFocus(event: any): void;
  onClick(): void;
  getDisplayOnlyTemplate(): JSX.Element;
  getInputTemplate(): JSX.Element;
  getType(): InputType;
  getIcon(): IconName;
  getDescriptorTemplate(): JSX.Element;
  getClearTemplate(): JSX.Element;
  getTogglePasswordTemplate(): JSX.Element;
  hasTextDescriptor(): boolean;
  hasLeftTextDescriptor(): boolean;
  hasRightTextDescriptor(): boolean;
  hasIconDescriptor(): boolean;
  hasClearButton(): boolean;
  getTextDescriptorLabel(): string;
  render(): JSX.Element;
}
