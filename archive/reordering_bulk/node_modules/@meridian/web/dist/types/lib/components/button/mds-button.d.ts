import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { InternalRoute } from '@meridian/core';
import { ButtonFontSize, ButtonSize, ButtonType, ButtonWidth, IconName, IconSize, TextOverflowType, TextWhiteSpace, TextWordBreak } from '@meridian/ui/extras';
export declare class ButtonComponent {
  /**
   * Whether the button should be displayed in a loading state. The button will also be disabled while this is true.
   */
  loading: boolean;
  /**
   * The label for the button. Provide this even for icononly buttons to support accessibility needs.
   */
  label: string;
  /**
   * The type of the button - determines the hierarchy
   */
  type: ButtonType;
  /**
   * Whether to disable the button
   */
  disabled: boolean;
  /**
   * The tooltip message to add to a disabled button
   */
  disabledreason: string;
  /**
   * Provide a filename to turn the button into a download link (used in conjuction with href property where the href property should point to the file path)
   */
  download: string;
  /**
   * The size of the icon.
   */
  iconsize: IconSize;
  /**
   * The icon to display as the only content on button. This should be an action oriented icon.
   */
  icononly: IconName;
  /**
   * The icon to display on the left of the button. This should be an action oriented icon.
   */
  iconleft: IconName;
  /**
   * The icon to display on the right of the button. This should be an action oriented icon.
   */
  iconright: IconName;
  /**
   * Providing a value renders the component as an anchor tag instead of a button with an href attribute pointing to the input value.
   */
  href: string;
  /**
   * Used in conjunction with the href or route attributes. Specifies the target attribute on the anchor tag.
   */
  target: string;
  /**
   * Provide details to route via the framework's router instead of a traditional href on an anchor tag.
   *
   * Use this to keep routing within a single page app instead of letting a whole page load occur.
   */
  route: InternalRoute;
  /**
   * Whether to stop navigation and let the consumer navigate.
   *
   * Use when you want the browser to recognize the URL, but want to handle routing yourself.
   */
  preventnavigation: boolean;
  /**
   * Use to define the size of the button, which affects things like the buttons height and padding
   */
  size: ButtonSize;
  /**
   * Add if the button should expand to the full-width of its container
   */
  width: ButtonWidth;
  /**
   * Whether text can wrap to multiple lines
   */
  nowrap: boolean;
  /**
   * The size of the text within the button.
   */
  private_fontsize: ButtonFontSize;
  /**
   * Overflow for long text
   */
  private_overflow: TextOverflowType;
  /**
   * To set the word break of the text
   */
  private_wordbreak: TextWordBreak;
  /**
   * To set the white space of the text
   */
  private_whitespace: TextWhiteSpace;
  /**
   * To set the destructive color to warning
   */
  private_warning: boolean;
  /**
   * Event emitted when the button is clicked
   */
  readonly mdsClick: EventEmitter<void>;
  /**
   * Event emitted when the button is blurred
   */
  readonly mdsBlur: EventEmitter<void>;
  /**
   * Event emitted when the button is focused
   */
  readonly mdsFocus: EventEmitter<void>;
  private readonly hostElement;
  private getLoading;
  private getIconWithName;
  private readonly onClick;
  private readonly onKeydown;
  private readonly linkOnClick;
  private readonly onFocus;
  private readonly onBlur;
  private isDisabledOrLoading;
  private getButtonRender;
  private getButtonLabelTemplate;
  private getButtonTruncateTemplate;
  private getRenderTag;
  render(): JSX.Element;
}
