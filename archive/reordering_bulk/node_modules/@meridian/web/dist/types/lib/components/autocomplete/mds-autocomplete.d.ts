import { JSX, Event, EventEmitter } from '../../../stencil-public-runtime';
import { PopoverContentPosition, PopoverContentPositionStrategy, PopoverContentWidth, AutocompleteTagLocation } from '@meridian/ui/extras';
/**
 * @deprecated
 * MDS will no longer support the autocomplete component.
 * Meridian will no longer be actively developing the autocomplete.
 * `mds-popover` and it's set of trigger and content components are the drop in replacement.
 */
export declare class AutocompleteComponent {
  /**
   * The list of items to show in the breadcrumb.
   *
   * Use {label: string, value: string}[] by passing an array of objects that have a value and label property. The "value" property must be unique for each item. You can optionally override the properties we use for label and value on your objects by setting the valueproperty and labelproperty props.
   */
  items: {}[] | string;
  /**
   * Property on items that is used to hold the value
   */
  valueproperty: string;
  /**
   * Property on items that is used to hold the label
   */
  labelproperty: string;
  /**
   * Whether to allow multiple values to be selected
   */
  multiselect: boolean;
  /**
   * Whether the autocomplete should be displayed with an error state
   */
  error: boolean;
  /**
   * Whether to display the "select all" option
   */
  selectall: boolean;
  /**
   * Whether to display the "create" option at the bottom when no option matches input
   */
  creatable: boolean;
  /**
   * The placeholder for the control
   */
  placeholder: string;
  /**
   * The width of the popover. Leave blank to keep it the same size as the input box.
   */
  popoverwidth: PopoverContentWidth;
  /**
   * Provide a custom width, in pixels, for the popover. Include 'px'. Note this only works when using popoverwidth='custom'
   */
  popovercustomwidth: string;
  /**
   * @deprecated DEPRECATED: Use selectbyvalue instead which does the same thing but is more appropriately named. Optionally set this to allow selecteditems to be the value rather than the entire object. Applies to both input of selecteditems and output of the value.
   */
  usestringasvalue: boolean;
  /**
   * Optionally set this to allow selecteditems to be the value rather than the entire object. Applies to both input of selecteditems and output of the value.
   */
  selectbyvalue: boolean;
  /**
   * Whether to show the autocomplete in a loading state. When loading, the autocomplete is disabled.
   */
  loading: boolean;
  /**
   * Whether to disable the autocomplete.
   */
  disabled: boolean;
  /**
   * Single selection only - prevents the user's ability to clear their selection. Allows it to function like the default HTML select if given a default value.
   */
  preventclear: boolean;
  /**
   * Single selection only - when an item is selected, the change event triggers, but the item is not shown in the input.
   */
  autoclear: boolean;
  /**
   * Where to display the autocomplete. 'bottomright' means the autocomplete's left edge will be aligned to the bottom left edge of its trigger.
   */
  position: PopoverContentPosition;
  /**
   * How to position the dropdown content. In most cases, leave at fixed which allows the popover content to break out of parent containers with overflows. In rare cases where you are using some type of transform, the fixed positioning may be off, in those cases, use 'absolute'.
   */
  positionstrategy: PopoverContentPositionStrategy;
  /**
   * Multiselect only - Where to display currently selected items.
   */
  taglocation: AutocompleteTagLocation;
  /**
   * The selected items of the autocomplete. Pass an array of objects or string representation of an array of objects for multiselect. Pass a single item or string of the value/object for single select.
   *
   * Use {}[] by passing an array of objects, {} for a single select, or a string representing the value of the selected item.
   */
  selecteditems: {}[] | {} | any;
  /**
   * Event emitted when the selected values of the autocomplete have changed.
   * With multiselect, emits either the list of selected items, selected values if using prop `usestringasvalue` or `selectbyvalue`, or an empty array if none selected
   * With single select, emits either the selected item, selected value if using prop `usestringasvalue` or `selectbyvalue`, or null if nothing selected
   */
  mdsAutocompleteChanged: EventEmitter<null | string | {} | {}[]>;
  /**
   * Event emitted when the value of the filter changes
   */
  mdsAutocompleteInputFiltered: EventEmitter<string>;
  /**
   * Event emitted when a new item has been created.
   */
  mdsAutocompleteItemCreated: EventEmitter<string>;
  /**
   * Event emitted when the autocomplete is opened
   */
  readonly mdsAutocompleteOpened: EventEmitter<void>;
  /**
   * Event emitted when the autocomplete is closed
   */
  readonly mdsAutocompleteClosed: EventEmitter<void>;
  /**
   * Event emitted when the autocomplete's show more is pressed to expand the selections shown
   */
  readonly mdsAutocompleteSelectionsExpanded: EventEmitter<void>;
  /**
   * Event emitted when the autocomplete's show less is pressed to condense the selections shown
   */
  readonly mdsAutocompleteSelectionsCondensed: EventEmitter<void>;
  hostElement: HTMLElement;
  private parsedItems;
  private parsedSelectedItems;
  private parsedSelectedItemsInteracted;
  private filterValue;
  private highlightedOption;
  private showMoreExpanded;
  private hiddenSelections;
  private resetScrollTopPosition;
  private updateScrollTopPosition;
  private firstRender;
  private clearSelectionElement;
  private menuOpen;
  private listItemHeight;
  private listHeight;
  private selectAllItem;
  private creatableItem;
  componentWillLoad(): void;
  componentDidLoad(): void;
  componentDidRender(): void;
  setValueProperty(): void;
  setLabelProperty(): void;
  setCreatableItem(): void;
  parseItems(): void;
  parseSelectedItems(): void;
  browserWindowResized(): void;
  handleKeyDown(ev: KeyboardEvent): void;
  private handleEnterSpace;
  private handleArrowDown;
  private handleArrowUp;
  private handleTab;
  private isClearSelectionButtonActive;
  setupInputListener(): void;
  private readonly inputListener;
  private readonly tabChangeListener;
  private forceUpdateIn;
  private setupTabListener;
  private highlightItem;
  private setPopoverListScrollTop;
  private getAutocompleteList;
  private getInitialOpenListScrollTop;
  private getKeydownListScrollTop;
  private setupShowMoreShowLessButton;
  private getAutocompleteItemVirtualScroll;
  private getFirstVisibleItem;
  private isSingleSelectAndHasSelection;
  private tagDismissClicked;
  private clearSelections;
  private isHighlightedItemSelectAll;
  private getIndexOfHighlightedItem;
  private highlightNextVisibleOption;
  private highlightPreviousVisibleOption;
  isItemHighlighted: (item: {}) => boolean;
  isItemVisible(item: {}): boolean;
  isItemSelected: (item: {}) => boolean;
  private removeItem;
  private selectItem;
  private setSelectedItems;
  private emitItem;
  private prepareSelectedItemsForEmit;
  handleItemClick: (item: {}, event?: Event) => void;
  private clearFilter;
  private hidePopover;
  private showPopover;
  private setFocusOnInput;
  private getInput;
  private getClearSelectionButton;
  private checkboxClicked;
  private filterChanged;
  private inputClicked;
  private showButtonClicked;
  private popoverOpened;
  private popoverClosed;
  private getItemByValue;
  private getFilteredParsedItems;
  private getHighlightableFilteredParsedItems;
  private singleSelectSelectionClick;
  private singleSelectClearClicked;
  private selectAllVisibleItems;
  private deselectAllVisibleItems;
  private areAllVisibleItemsSelected;
  private areSomeVisibleItemsSelected;
  getSelectedItems: () => {}[];
  getHighlightedItem: () => {};
  getFilterValue: () => string;
  getListItemLabel: (itemLabel: string) => string;
  selectAllRowClicked: (event: Event) => void;
  selectAllCheckboxClicked: (event: any) => void;
  private getPlaceholder;
  private getSingleselectSelectionTemplate;
  private shouldShowSingleSelectClear;
  private shouldSelectByValue;
  private getSingleselectClearTemplate;
  private getChevronTemplate;
  private getLoadingTemplate;
  private getMultiselectSelectionsTemplate;
  private getFooterTemplate;
  private canClearSelections;
  render(): JSX.Element;
}
