import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { FlyoutSize, FlyoutType } from '@meridian/ui/extras';
export declare class FlyoutComponent {
  /**
   * The size of the flyout
   */
  size: FlyoutSize;
  /**
   * The id for the flyout option. If one is not supplied, a UUID will be assigned.
   */
  flyoutid: string;
  /**
   * Loading state for the flyout disables all escape routes except ESC key.
   */
  loading: boolean;
  /**
   * The boolean for whether the 'WalkMe' component should be hidden when the flyout is open.
   */
  hidewalkme: boolean;
  /**
   * The id for the 'WalkMe' element.
   */
  walkmeid: string;
  /**
   * Whether the flyout should initially render as open
   */
  startasopen: boolean;
  /**
   * Type of flyout (e.g., whether the page is scrollable while the flyout is open)
   */
  type: FlyoutType;
  /**
   * Prevent the flyout from closing through interaction (X, ESC, Cancel). All closing will need to be done programatically with the `hide` method.
   */
  preventclose: boolean;
  /**
   * Event emitted when the flyout is attempted to be closed but preventclose is true
   */
  readonly mdsFlyoutCloseRequested: EventEmitter<void>;
  /**
   * Event emitted when the flyout is opened
   */
  readonly mdsFlyoutOpened: EventEmitter<void>;
  /**
   * Event emitted when the flyout is closed
   */
  readonly mdsFlyoutClosed: EventEmitter<void>;
  open: boolean;
  closing: boolean;
  /**
   * Call this to programmatically show the flyout.
   */
  show(): Promise<void>;
  /**
   * Call this to programmatically hide the flyout.
   */
  hide(respectPreventClose?: boolean): Promise<void>;
  disconnectedCallback(): void;
  hostElement: HTMLStencilElement;
  private focusedElement;
  handleKeyDown(event: KeyboardEvent): void;
  handleEscape(ev: KeyboardEvent): void;
  componentWillLoad(): void;
  componentDidLoad(): void;
  showOverlay(): boolean;
  private getFlyoutContentsTemplate;
  private isFlyoutOpen;
  private setBodyTag;
  private resetBodyTag;
  private startClosingFlyout;
  private closeFlyout;
  private openFlyout;
  render(): JSX.Element;
}
