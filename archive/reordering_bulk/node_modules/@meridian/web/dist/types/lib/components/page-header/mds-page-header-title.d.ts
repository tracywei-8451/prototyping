import { EventEmitter, JSX } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { HeadingOverflowType, HeadingWordBreak } from '@meridian/ui/extras';
export declare class PageHeaderMainComponent {
  /**
   * Title for the page header.
   */
  titletext: string;
  /**
   * Set to add an ellipsis to the end of a one-line string.
   */
  overflow: HeadingOverflowType;
  /**
   * Title word break for the card
   */
  titlewordbreak: HeadingWordBreak;
  /**
   * Allows the user to edit the title with a slotted form field.
   */
  editable: boolean;
  /**
   * The ID for the page header title section. This ID can be used
   * with the edit mode to dispatch the doneEditing() method upon
   * emitting the MdsPageHeaderConfirmEdit event (see code example)
   */
  titleid: string;
  /**
   * Event emitted when the edit title button is clicked.
   */
  mdsPageHeaderStartTitleEdit: EventEmitter<void>;
  /**
   * Event emitted when the cancel button is clicked while editing the title.
   */
  mdsPageHeaderCancelTitleEdit: EventEmitter<void>;
  /**
   * Event emitted when the check button is clicked while editing the title.
   */
  mdsPageHeaderConfirmTitleEdit: EventEmitter<void>;
  /**
   * Method used to save a title change upon clicking the checkmark and hide the edit mode.
   */
  doneEditing(): Promise<void>;
  editing: boolean;
  hostElement: HTMLStencilElement;
  componentWillLoad(): void;
  private getHeadingTemplate;
  private getEditable;
  private showEdit;
  private hideEdit;
  private cancelEdit;
  private confirmEdit;
  render(): JSX.Element;
}
