import { JSX } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
export declare class ModalHeaderComponent {
  /**
   * Primary header of modal
   */
  header: string;
  /**
   * Secondary header of modal
   */
  subheader: string;
  private_setCloseable(closeable: boolean): Promise<void>;
  private_setError(error: string): Promise<void>;
  hostElement: HTMLStencilElement;
  closeable: boolean;
  error: string;
  private onClick;
  componentWillLoad(): void;
  render(): JSX.Element;
}
