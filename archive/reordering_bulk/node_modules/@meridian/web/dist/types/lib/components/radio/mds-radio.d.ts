import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { SelectorInterface } from '../../base/mds-selector.interface';
import { MdsRadioChangedEvent } from './radio-changed-event';
import { MdsRadioClickEvent } from './radio-click-event';
import { RadioAlignment, RadioPosition, RadioSize, RadioType, TextFontWeight, TextOverflowType, TextWordBreak } from '@meridian/ui/extras';
export declare class RadioComponent implements SelectorInterface {
  /**
   * The id for the radio option. If one is not supplied, a UUID will be assigned.
   */
  radioid: string;
  /**
   * Label for the radio control
   */
  label: string;
  /**
   * The value of the radio when selected.
   */
  value: any;
  /**
   * The size of the radio label. Corresponds to the mds-text sizes.
   */
  size: RadioSize;
  /**
   * The font weight of the radio label.
   */
  fontweight: TextFontWeight;
  /**
   * Where the radio button sits in in respect to slot when type card.
   * Note: Default position orients the slot below the radio.
   * Position "none" orients the slot like the default position but with no visible radio.
   */
  radioposition: RadioPosition;
  /**
   * How the radio container is horizontally positioned when type card. Default is top aligned.
   */
  radioalignment: RadioAlignment;
  /**
   * Set to add an ellipsis to the end of a one-line string
   */
  overflow: TextOverflowType;
  /**
   * The word break style for the label.
   */
  wordbreak: TextWordBreak;
  /**
   * Name for the card's form control.
   */
  name: string;
  /**
   * Whether to disable the radio control.
   */
  disabled: boolean;
  /**
   * The tooltip message to add to a disabled radio button.
   */
  disabledreason: string;
  /**
   * @deprecated DEPRECATED: Include to extend the component to have a slot where any generic content can be passed in.
   * Note: do not slot elements with any click handling as they will interfere with the radio of this component.
   * Use the type prop (card) instead.
   */
  hasdescription: boolean;
  /**
   * Type of radio
   */
  type: RadioType;
  /**
   * Whether the radio is required.
   */
  required: boolean;
  /**
   * Whether the control is selected.
   */
  checked: boolean;
  /**
   * @private for internal use only
   *
   * The tabindex
   */
  private_tabindex: number;
  /**
   * Whether label expands full width/height of component (e.g., card).
   */
  expanded: boolean;
  /**
   * @private for internal use only
   *
   * Whether the checkbox is focusable when disabled.
   */
  private_disabledfocus: boolean;
  /**
   * @private for internal use only
   *
   * Whether the radio should be formatted as a list item.
   */
  private_listitem: boolean;
  /**
   * Event emitted when the control is selected or unselected.
   * If selected, emits value, else ''.
   */
  mdsRadioChanged: EventEmitter<any>;
  /**
   * Event emitted when the radio is clicked. Returns the original click event
   * as well as the radio's value.
   */
  readonly mdsClick: EventEmitter<MdsRadioClickEvent>;
  /**
   * Private event, not meant to be used by consumers. Event emitted when the radio value is changed with the name of the radio group that changed. Bubbles up to window.
   */
  readonly private_mdsRadioChanged: EventEmitter<MdsRadioChangedEvent>;
  hostElement: HTMLElement;
  externalFormValue: unknown;
  private_updateChecked(selected: boolean, emitChange?: boolean): Promise<void>;
  private_updateValue(value: string): Promise<void>;
  componentWillLoad(): void;
  componentDidLoad(): void;
  private getTabIndexBasedOnDisabledState;
  private isDisabledAndShouldBeFocusable;
  getRadioTemplate(): string;
  isTypeCard(): boolean;
  getRadioDescriptionTemplate(): string;
  anotherRadioChanged(event: CustomEvent<MdsRadioChangedEvent>): void;
  private readonly onInputClick;
  private readonly onKeyDown;
  private readonly onHostClick;
  private toggleChecked;
  private getInput;
  private getRadioWrapper;
  render(): JSX.Element;
}
