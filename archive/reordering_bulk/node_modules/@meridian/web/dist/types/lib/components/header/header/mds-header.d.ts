import { JSX } from '../../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../../stencil-public-runtime';
export declare class HeaderComponent {
  /**
   * Set to the browser width that you want the header to switch to full size
   */
  fullbreakpoint: number;
  /**
   * Set to the max browser width that you want the header to stretch horizontally. Leave blank to fill the entire browser width.
   */
  maxwidth: number;
  /**
   * Internal only - determine if header is displayed in mobile or full
   */
  private_fullheader: boolean;
  /**
   * If true, hides the navigation menu and menu trigger button in the header.
   * Accepts boolean or string values ("true"/"false").
   */
  isMenuHidden: string | boolean;
  /**
   * If true, applies box-shadow to the header. If false, no shadow is shown.
   */
  removeShadow: boolean;
  hostElement: HTMLStencilElement;
  menuExpanded: boolean;
  hasMaxWidth: boolean;
  browserAtMaxWidth: boolean;
  fullBreakpointChanged(): void;
  maxWidthChanged(): void;
  browserWindowResized(event: Event): void;
  getWindowWidth(): number;
  private_setFullHeader(windowWidth: number): void;
  setMaxWidth(windowWidth: number): void;
  setFullHeaderOnElement(selector: string): void;
  setMenuExpandedOnElement(selector: string): void;
  componentWillLoad(): void;
  componentDidLoad(): void;
  private readonly onMenuClick;
  render(): JSX.Element;
}
