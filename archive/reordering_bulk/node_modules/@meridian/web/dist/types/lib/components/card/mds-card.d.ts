import { InternalRoute } from '@meridian/core';
import { ButtonType, CardTitleSize, HeadingWordBreak } from '@meridian/ui/extras';
import { EventEmitter, JSX } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { SelectorInterface } from '../../base/mds-selector.interface';
import { MdsRadioChangedEvent } from '../radio/radio-changed-event';
import { MdsSecondaryAction } from './secondary-action';
export declare class CardComponent implements SelectorInterface {
  /**
   * The appearance of the card. Only use the horizontal layout when the card has an attached media object.
   */
  horizontal: boolean;
  /**
   * @deprecated DEPRECATED: Whether the card can be selected.
   * Use the type prop (card) on the checkbox or radio instead.
   */
  selectable: boolean;
  /**
   * @deprecated DEPRECATED: Label for the card when it is not selected
   * Use the type and/or label prop on the checkbox or radio instead.
   */
  unselectedlabel: string;
  /**
   * @deprecated DEPRECATED: Label for the card when it is not selected
   * Use the type and/or label prop on the checkbox or radio instead.
   */
  selectedlabel: string;
  /**
   * @deprecated DEPRECATED: Determines whether to show a checkbox or radio.
   * Use the type prop (card) on the checkbox or radio instead.
   */
  multiselect: boolean;
  /**
   * @deprecated DEPRECATED: The value of the card when selected.
   * Required if using radio card selection (selectable is true and multiselect is false).
   * Use the type prop (card) on the checkbox or radio instead.
   */
  value: any;
  /**
   * Title for the card
   */
  titletext: string;
  /**
   * Title size for the card
   */
  titlesize: CardTitleSize;
  /**
   * Title word break for the card
   */
  titlewordbreak: HeadingWordBreak;
  /**
   * Name for the card's form control. Required if using radio card selection (selectable is true and multiselect is false).
   */
  name: string;
  /**
   * Does the card have an action when clicking on the title
   */
  hastitleaction: boolean;
  /**
   * Whether to disable the card title
   */
  titledisabled: boolean;
  /**
   * @deprecated DEPRECATED: Whether to disable the card selectability - used along with "selectable".
   * Use the type and disabled prop on the checkbox or radio instead.
   */
  selectiondisabled: boolean;
  /**
   * @deprecated DEPRECATED: Whether clicking the primary action button will select the card.
   * Use the type prop (card) on the checkbox or radio instead.
   */
  primaryactionselectscard: boolean;
  /**
   * The label for the primary action button. Set this if you want a primary action button or leave blank to exclude the button.
   */
  primaryactionlabel: string;
  /**
   * Whether the Primary Action Button should be displayed in a loading state
   */
  primaryactionloading: boolean;
  /**
   * Whether to disable the Primary Action Button - used along with "primaryactionlabel"
   */
  primaryactiondisabled: boolean;
  /**
   * The tooltip message to add to the Secondary Action Button if it is disabled
   */
  primaryactiondisabledreason: string;
  /**
   * The button style of the Primary Action Button
   */
  primaryactiontype: ButtonType;
  /**
   * Providing a value renders the Primary Action Button as an anchor tag instead of a button with an href attribute pointing to the input value.
   */
  primaryactionhref: string;
  /**
   * Used in conjunction with the href or route attributes. Specifies the target attribute on the anchor tag.
   */
  primaryactiontarget: string;
  /**
   * Provide details to route via the framework's router instead of a traditional href on an anchor tag.
   *
   * Use this to keep routing within a single page app instead of letting a whole page load occur.
   */
  primaryactionroute: InternalRoute;
  /**
   * The list of actions and labels for the action menu.
   *
   * Use MdsSecondaryAction[] by passing an array of objects.
   */
  secondaryactions: MdsSecondaryAction[];
  /**
   * Whether to disable the card secondary actions - used along with "secondaryactions".
   */
  secondaryactionsdisabled: boolean;
  /**
   * The tooltip message to add to the Primary Action Button if it is disabled
   */
  secondaryactionsdisabledreason: string;
  /**
   * Whether the card is selected
   */
  checked: boolean;
  /**
   * Event emitted when any of the secondary actions in the action menu are clicked. Emits the label of the action that was clicked.
   */
  mdsSecondaryActionClick: EventEmitter<string>;
  /**
   * Event emitted when the primary action button is clicked.
   */
  mdsPrimaryActionClick: EventEmitter<void>;
  /**
   * Event emitted when the title is clicked.
   */
  mdsTitleActionClick: EventEmitter<void>;
  /**
   * Event emitted when the card is selected or unselected.
   * For multi-select, if selected, emits true, else false.
   * For single-select, if selected, emits value, else ''.
   */
  mdsSelectionChanged: EventEmitter<any>;
  hostElement: HTMLStencilElement;
  externalFormValue: unknown;
  private_updateValue(value: unknown): Promise<void>;
  radioId: string;
  componentWillLoad(): void;
  componentDidLoad(): void;
  anotherRadioChanged(event: CustomEvent<MdsRadioChangedEvent>): void;
  private hasHeaderActions;
  private getCheckbox;
  private getRadio;
  private getCheckboxInput;
  private getRadioInput;
  private readonly onActionMenuItemClick;
  private readonly onPrimaryActionButtonClick;
  private readonly onTitleActionClick;
  private readonly onInputClick;
  private toggleCardChecked;
  private getCardMediaTemplate;
  private getCardFooterTemplate;
  private getSelectionOptionTemplate;
  private getSecondaryActionsTemplate;
  private getPrimaryActionButtonTemplate;
  render(): JSX.Element;
}
