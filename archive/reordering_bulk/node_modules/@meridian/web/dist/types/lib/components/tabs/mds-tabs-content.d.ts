import { JSX } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
export declare class TabsContentComponent {
  /**
   * A unique tab id must be provided for each `mds-tabs-label`. The same id should be given to the corresponding `mds-tabs-content` element.
   */
  tabnum: string | number;
  /**
   * Tabs fill up vertical space of wrapping container
   */
  private_fullheight: boolean;
  hostElement: HTMLStencilElement;
  parentUUID: any;
  isActive: boolean;
  private_setActive(active: boolean): Promise<void>;
  private_setParentUUID(UUID: string): Promise<void>;
  private_setFullHeight(private_fullheight: boolean): Promise<void>;
  componentWillLoad(): void;
  render(): JSX.Element;
}
