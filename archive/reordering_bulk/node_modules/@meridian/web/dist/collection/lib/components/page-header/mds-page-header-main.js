import { h, Host } from '@stencil/core';
export class PageHeaderMainComponent {
  constructor() {
    this.pageheaderid = undefined;
  }
  componentWillLoad() {
    if (this.pageheaderid) {
      this.hostElement.id = this.pageheaderid;
    }
  }
  render() {
    return (h(Host, { slot: "page-header-main" }, h("div", { class: "mds-page-header-main" }, h("slot", { name: "page-header-title" }), h("slot", { name: "page-header-actions" }))));
  }
  static get is() { return "mds-page-header-main"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_page-header-main.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_page-header-main.css"]
    };
  }
  static get properties() {
    return {
      "pageheaderid": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "string",
          "resolved": "string",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The ID for the page header main section. This ID can be used\nwith the edit mode to dispatch the doneEditing() method upon\nemitting the MdsPageHeaderConfirmEdit event (see code example)"
        },
        "attribute": "pageheaderid",
        "reflect": false
      }
    };
  }
  static get elementRef() { return "hostElement"; }
}
