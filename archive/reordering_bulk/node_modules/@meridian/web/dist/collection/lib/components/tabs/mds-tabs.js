import { h, Host, } from '@stencil/core';
import { MdsStringUtility, MdsEventUtility, MdsShadowDomUtility, } from '@meridian/core';
import { MdsTabsColorEnum } from '@meridian/ui/extras';
export class TabsComponent {
  constructor() {
    this.aslinks = false;
    this.color = MdsTabsColorEnum.default;
    this.full = false;
    this.fullheight = false;
    this.fullwidth = false;
    this.nav = false;
    this.selectbytabnum = false;
    this.selectedindex = 0;
    this.selectedtabnum = undefined;
    this.vertical = false;
    this.private_UUID = MdsStringUtility.getUUID();
  }
  asLinksChanged() {
    // Change child aslinks property
    this.getTabs().forEach((item) => {
      item.private_setAsLinks(this.aslinks);
    });
  }
  fullWidthChanged() {
    // Change child fullwidth property
    this.getTabs().forEach((item) => {
      item.private_setFullWidth(this.fullwidth);
    });
  }
  fullheightChanged() {
    // Change child full property
    this.getContent().forEach((item) => {
      item.private_setFullHeight(this.fullheight);
    });
  }
  navChanged() {
    // Change child nav property, used for navigation-style tabs
    this.getTabs().forEach((item) => {
      item.private_setNav(this.nav);
    });
  }
  selectedIndexChanged() {
    if (!this.selectbytabnum) {
      this.selectTab(this.selectedindex, false);
    }
  }
  selectedTabnumChanged() {
    if (this.selectbytabnum) {
      this.selectTabByTabnum(this.selectedtabnum, false);
    }
  }
  verticalChanged() {
    // Change child vertical property
    this.getTabs().forEach((item) => {
      item.private_setVertical(this.vertical);
    });
  }
  handleKeyDown(ev) {
    if (this.isFocusOnTab()) {
      if (!this.aslinks) {
        this.handleKeyNext(ev);
        this.handleKeyPrevious(ev);
        this.handleKeyMoveToContent(ev);
      }
      this.handleEnterSpace(ev);
    }
  }
  handleKeyNext(ev) {
    if (this.keyNext(ev.key)) {
      const newIndex = this.selectedindex < this.getTabs().length - 1
        ? this.selectedindex + 1
        : this.selectedindex;
      if (newIndex !== this.selectedindex) {
        const newParent = this.getTabs()[newIndex];
        if (newParent) {
          if (!newParent['disabled']) {
            this.selectTabAndFocus(newIndex);
          }
          else if (newParent['disabled'] &&
            newIndex < this.getTabs().length) {
            this.selectTabAndFocus(newIndex + 1);
          }
        }
      }
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleKeyPrevious(ev) {
    if (this.keyPrevious(ev.key)) {
      const newIndex = this.selectedindex > 0 ? this.selectedindex - 1 : this.selectedindex;
      if (newIndex !== this.selectedindex) {
        const newParent = this.getTabs()[newIndex];
        if (newParent) {
          if (!newParent['disabled']) {
            this.selectTabAndFocus(newIndex);
          }
          else if (newParent['disabled'] && newIndex > 0) {
            this.selectTabAndFocus(newIndex - 1);
          }
        }
      }
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleKeyMoveToContent(ev) {
    if (this.keyMoveToContent(ev.key)) {
      this.focusOnContent(this.selectedindex);
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleEnterSpace(ev) {
    if (ev.key === ' ' || ev.key === 'Enter') {
      MdsShadowDomUtility.getActiveElement().click();
      MdsEventUtility.cancelEvent(ev);
    }
  }
  onTabClick(event) {
    if (event.detail.tabParentId === this.private_UUID) {
      this.selectTabByTabnum(event.detail.tabnum);
      MdsEventUtility.cancelEvent(event);
    }
  }
  componentWillLoad() {
    if (!this.hostElement.id) {
      this.hostElement.id = MdsStringUtility.getUUID();
    }
    // Set properties on children
    this.getTabs().forEach((item) => {
      item.private_setParentUUID(this.private_UUID);
      item.private_setAsLinks(this.aslinks);
      item.private_setVertical(this.vertical);
      item.private_setColor(this.color);
      item.private_setFullWidth(this.full || this.fullwidth);
      item.private_setNav(this.nav);
    });
    this.getContent().forEach((item) => {
      item.private_setParentUUID(this.private_UUID);
      item.private_setFullHeight(this.fullheight);
    });
  }
  componentDidLoad() {
    this.selectTabConditionally();
  }
  selectTabByTabnum(tabnum, emit = true) {
    const tabs = this.getTabs();
    if (tabs.length > 0) {
      this.selectTab(this.getIndexOfSelectedTab(tabnum, tabs), emit);
    }
  }
  selectTab(index, emit = true) {
    this.selectedindex = index;
    const tab = this.getTabs()[index];
    if (tab) {
      this.selectedtabnum = tab.tabnum;
    }
    this.getTabs().forEach((item, tabIndex) => {
      item.private_setActive(this.selectedindex === tabIndex);
    });
    this.getContent().forEach((item, contentIndex) => {
      item.private_setActive(this.selectedindex === contentIndex);
    });
    if (emit) {
      this.mdsTabChanged.emit(this.selectbytabnum
        ? this.getTabs()[this.selectedindex].tabnum
        : this.selectedindex);
    }
  }
  getIndexOfSelectedTab(tabnum, tabs) {
    // Find the item in the list
    let itemIndex = -1;
    tabs.forEach((item, index) => {
      // tslint:disable-next-line:triple-equals
      if (item.tabnum == tabnum) {
        itemIndex = index;
      }
    });
    // Didn't find the element
    if (itemIndex === -1) {
      return 0;
    }
    return itemIndex;
  }
  isFocusOnTab() {
    return ([].slice
      .call(this.getTabs())
      .filter((tab) => document.activeElement === tab).length > 0);
  }
  keyNext(key) {
    return ((!this.vertical && !this.nav && key === 'ArrowRight') ||
      ((this.vertical || this.nav) && key === 'ArrowDown'));
  }
  keyPrevious(key) {
    return ((!this.vertical && !this.nav && key === 'ArrowLeft') ||
      ((this.vertical || this.nav) && key === 'ArrowUp'));
  }
  keyMoveToContent(key) {
    return ((!this.vertical && !this.nav && key === 'ArrowDown') ||
      ((this.vertical || this.nav) && key === 'ArrowRight'));
  }
  selectTabAndFocus(index) {
    this.selectTab(index);
    this.getTabByIndex(index).focus();
  }
  getContentByIndex(index) {
    return this.getContent()[index].shadowRoot.querySelector('.mds-tabs-content');
  }
  getTabByIndex(index) {
    return this.getTabs()[index].shadowRoot.querySelector('.mds-tabs-label');
  }
  focusOnContent(index) {
    this.getContentByIndex(index).focus();
  }
  getTabs() {
    return Array.from(this.hostElement.querySelectorAll(`#tabs-${this.private_UUID} > mds-tabs-label`));
  }
  getContent() {
    return Array.from(this.hostElement.querySelectorAll(`#tabs-${this.private_UUID} > mds-tabs-content`));
  }
  selectTabConditionally() {
    if (this.selectbytabnum) {
      this.selectTabByTabnum(this.selectedtabnum, false);
    }
    else {
      this.selectTab(this.selectedindex, false);
    }
  }
  tabSlotChanged() {
    this.selectTabConditionally();
    this.getTabs().forEach((tab) => tab.private_updateLast());
  }
  render() {
    return (h(Host, { id: `tabs-${this.private_UUID}` }, h("div", { class: {
        'mds-tabs': true,
        'mds-tabs--full-height': this.fullheight,
      } }, h("div", { class: {
        'mds-tabs__list': true,
        'mds-tabs__list--full-width': this.fullwidth || this.full,
      }, role: "tablist", id: `tab-list-${this.private_UUID}` }, h("slot", { name: "tab", onSlotchange: () => this.tabSlotChanged() })), h("slot", { name: "content" }))));
  }
  static get is() { return "mds-tabs"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_tabs.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_tabs.css"]
    };
  }
  static get properties() {
    return {
      "aslinks": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "If the tabs will be used as links, you need to set this to true to enable proper tab indexing and keyboard navigation on the tabs."
        },
        "attribute": "aslinks",
        "reflect": false,
        "defaultValue": "false"
      },
      "color": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "TabsColor",
          "resolved": "\"default\" | \"subtle\"",
          "references": {
            "TabsColor": {
              "location": "import",
              "path": "@meridian/ui/extras"
            }
          }
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The color of the tab highlighting features."
        },
        "attribute": "color",
        "reflect": false,
        "defaultValue": "MdsTabsColorEnum.default"
      },
      "full": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [{
              "name": "deprecated",
              "text": "DEPRECATED: Tabs fill up horizontal space of wrapping container. Use fullwidth instead."
            }],
          "text": ""
        },
        "attribute": "full",
        "reflect": false,
        "defaultValue": "false"
      },
      "fullheight": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Tabs fill up vertical space of wrapping container."
        },
        "attribute": "fullheight",
        "reflect": true,
        "defaultValue": "false"
      },
      "fullwidth": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Tabs fill up horizontal space of wrapping container."
        },
        "attribute": "fullwidth",
        "reflect": false,
        "defaultValue": "false"
      },
      "nav": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Vertical display variant intended for navigation."
        },
        "attribute": "nav",
        "reflect": true,
        "defaultValue": "false"
      },
      "selectbytabnum": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Both input and output will be based on tabnum instead of index.\nWhen true: for tab assignment, selectedtabnum will be used instead of selectedindex.\nFor output on mdsTabChanged, the selectedtabnum will be emitted instead of selectedindex."
        },
        "attribute": "selectbytabnum",
        "reflect": false,
        "defaultValue": "false"
      },
      "selectedindex": {
        "type": "number",
        "mutable": false,
        "complexType": {
          "original": "number",
          "resolved": "number",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The tab index that should be selected."
        },
        "attribute": "selectedindex",
        "reflect": false,
        "defaultValue": "0"
      },
      "selectedtabnum": {
        "type": "any",
        "mutable": false,
        "complexType": {
          "original": "string | number",
          "resolved": "number | string",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The tab that should be selected (by tabnum). Accepts number or string. *Must pass selectbytabnum as true to enable this property."
        },
        "attribute": "selectedtabnum",
        "reflect": false
      },
      "vertical": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Display the tabs to the left of the content menu. Default has tabs above the content."
        },
        "attribute": "vertical",
        "reflect": true,
        "defaultValue": "false"
      },
      "private_UUID": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "string",
          "resolved": "string",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [{
              "name": "internal",
              "text": undefined
            }],
          "text": "The UUID of the tabs. Used to give tab and content unique identifiers for accessibility."
        },
        "attribute": "private_-u-u-i-d",
        "reflect": false,
        "defaultValue": "MdsStringUtility.getUUID()"
      }
    };
  }
  static get events() {
    return [{
        "method": "mdsTabChanged",
        "name": "mdsTabChanged",
        "bubbles": false,
        "cancelable": true,
        "composed": true,
        "docs": {
          "tags": [],
          "text": "Event emitted when a tab is changed.\nIf selectbytabnum is false, emits the index of the tab that is now selected.\nIf selectbytabnum is true, emits the tabnum of the tab that is now selected."
        },
        "complexType": {
          "original": "string | number",
          "resolved": "number | string",
          "references": {}
        }
      }];
  }
  static get elementRef() { return "hostElement"; }
  static get watchers() {
    return [{
        "propName": "aslinks",
        "methodName": "asLinksChanged"
      }, {
        "propName": "full",
        "methodName": "fullWidthChanged"
      }, {
        "propName": "fullwidth",
        "methodName": "fullWidthChanged"
      }, {
        "propName": "fullheight",
        "methodName": "fullheightChanged"
      }, {
        "propName": "nav",
        "methodName": "navChanged"
      }, {
        "propName": "selectedindex",
        "methodName": "selectedIndexChanged"
      }, {
        "propName": "selectedtabnum",
        "methodName": "selectedTabnumChanged"
      }, {
        "propName": "vertical",
        "methodName": "verticalChanged"
      }];
  }
  static get listeners() {
    return [{
        "name": "keydown",
        "method": "handleKeyDown",
        "target": undefined,
        "capture": false,
        "passive": false
      }, {
        "name": "private_mdsTabClick",
        "method": "onTabClick",
        "target": undefined,
        "capture": false,
        "passive": false
      }];
  }
}
