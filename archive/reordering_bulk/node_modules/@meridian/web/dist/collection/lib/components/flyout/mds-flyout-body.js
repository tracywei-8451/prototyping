import { h, Host } from '@stencil/core';
export class FlyoutBodyComponent {
  constructor() {
    this.softedges = true;
  }
  render() {
    return (h(Host, { slot: "flyout-body" }, h("div", { class: {
        'mds-flyout-body': true,
        'mds-flyout-body--no-soft-edges': !this.softedges,
      } }, h("slot", null))));
  }
  static get is() { return "mds-flyout-body"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_flyout-body.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_flyout-body.css"]
    };
  }
  static get properties() {
    return {
      "softedges": {
        "type": "boolean",
        "mutable": false,
        "complexType": {
          "original": "boolean",
          "resolved": "boolean",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Whether body has soft top/bottom edges"
        },
        "attribute": "softedges",
        "reflect": true,
        "defaultValue": "true"
      }
    };
  }
  static get elementRef() { return "hostElement"; }
}
