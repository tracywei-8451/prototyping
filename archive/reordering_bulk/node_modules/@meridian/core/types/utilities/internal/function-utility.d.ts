type UnknownFunction = (...args: any[]) => unknown;
export declare class MdsFunctionUtility {
    static repeatFor(executionFn: UnknownFunction, totalMs: number, iterateMs: number, ...args: any[]): void;
    /**
     *
     * @param executionFn
     * @param checkFn
     * @param iterateMs
     * @param totalMs the max time to try this... a way out
     */
    static executeOnceTrue(executionFn: () => unknown, checkFn: () => boolean, iterateMs: number, totalMs?: any): void;
    static executeUntilTrue(executionFn: () => unknown, checkFn: () => boolean, iterateMs: number): void;
    static focusInputElementOnDelay(focusElement: HTMLElement): void;
}
export {};
