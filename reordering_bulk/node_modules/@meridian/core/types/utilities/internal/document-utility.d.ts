import { Renderer2 } from '@angular/core';
export declare class MdsDocumentUtility {
    /**
     *
     * @param preventScrollbarJump Whether to adjust the page width (padding) to account for scrollbar jump
     * @param walkmeId Optionally provide a walkmeId which will be adjusted for the change to the scrollbar
     */
    static hideBodyScroll(preventScrollbarJump?: boolean, walkmeId?: string): void;
    /**
     *
     * @param preventScrollbarJump Whether to adjust the page width (padding) to account for scrollbar jump
     * @param walkmeId Optionally provide a walkmeId which will be adjusted for the change to the scrollbar
     */
    static showBodyScroll(preventScrollbarJump?: boolean, walkmeId?: string): void;
    static hideWalkMeComponent(walkmeId: string): void;
    static showWalkMeComponent(walkmeId: string): void;
    static getScrollbarWidth(): number;
    static preventUserSelect(renderer: Renderer2): void;
    static enableUserSelect(renderer: Renderer2): void;
    private static documentContainsBody;
}
