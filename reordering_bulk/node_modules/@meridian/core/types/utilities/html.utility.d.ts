export declare class MdsSafeHtmlString {
    readonly html: string;
    constructor(config: {
        text: string;
        style?: {
            match: string;
            elementTag: string;
            classes?: string[];
            styles?: string[];
        };
    });
}
export declare class MdsHtmlUtility {
    static textStyler: {
        new (): {};
        /** Defines a custom text styler un-respected by Meridian practices */
        custom: ({ elementTag, classes, styles, }: {
            elementTag: string;
            classes?: string[];
            styles?: string[];
        }) => ({ text, match }: {
            text: string;
            match: string;
        }) => MdsSafeHtmlString;
        /** Highlight the matched text, used to draw attention to text */
        bold: ({ text, match }: {
            text: string;
            match: string;
        }) => MdsSafeHtmlString;
    };
    static html: (inputString: string) => MdsSafeHtmlString;
}
