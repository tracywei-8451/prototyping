import { IconName } from '@meridian/ui/extras';
import { JSX } from '../../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../../stencil-public-runtime';
export declare class HeaderDropdownComponent {
  /**
   * Do not add the slot definition. Use when part of another component.
   */
  private_excludeslot: boolean;
  /**
   * The icon to use for the dropdown trigger
   */
  icon: IconName;
  /**
   * The tooltip to use for the hover over the dropdown
   */
  tooltip: string;
  hostElement: HTMLStencilElement;
  dropdownOpen: boolean;
  private getHeaderDropdownTemplate;
  private getHostTemplate;
  render(): JSX.Element;
}
