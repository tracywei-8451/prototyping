import { JSX } from '../../../stencil-public-runtime';
import { CheckboxGroupLayoutType } from '@meridian/ui/extras';
export declare class CheckboxGroupComponent {
  /**
   * The type of layout for the checkbox group
   */
  type: CheckboxGroupLayoutType;
  /**
   * The width of the columns if displayed in a column layout
   */
  columnwidth: number;
  getColumnWidth(): any;
  render(): JSX.Element;
}
