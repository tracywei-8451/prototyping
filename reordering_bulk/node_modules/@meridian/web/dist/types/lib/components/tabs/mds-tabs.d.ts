import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { MdsTabClickedEvent } from './tab-clicked-event';
import { TabsColor } from '@meridian/ui/extras';
export declare class TabsComponent {
  /**
   * If the tabs will be used as links, you need to set this to true to enable proper tab indexing and keyboard navigation on the tabs.
   */
  aslinks: boolean;
  /**
   * The color of the tab highlighting features.
   */
  color: TabsColor;
  /**
   * @deprecated DEPRECATED: Tabs fill up horizontal space of wrapping container. Use fullwidth instead.
   */
  full: boolean;
  /**
   * Tabs fill up vertical space of wrapping container.
   */
  fullheight: boolean;
  /**
   * Tabs fill up horizontal space of wrapping container.
   */
  fullwidth: boolean;
  /**
   * Vertical display variant intended for navigation.
   */
  nav: boolean;
  /**
   * Both input and output will be based on tabnum instead of index.
   * When true: for tab assignment, selectedtabnum will be used instead of selectedindex.
   * For output on mdsTabChanged, the selectedtabnum will be emitted instead of selectedindex.
   */
  selectbytabnum: boolean;
  /**
   * The tab index that should be selected.
   */
  selectedindex: number;
  /**
   * The tab that should be selected (by tabnum). Accepts number or string. *Must pass selectbytabnum as true to enable this property.
   */
  selectedtabnum: string | number;
  /**
   * Display the tabs to the left of the content menu. Default has tabs above the content.
   */
  vertical: boolean;
  /**
   * The UUID of the tabs. Used to give tab and content unique identifiers for accessibility.
   * @internal
   */
  private_UUID: string;
  /**
   * Event emitted when a tab is changed.
   * If selectbytabnum is false, emits the index of the tab that is now selected.
   * If selectbytabnum is true, emits the tabnum of the tab that is now selected.
   */
  mdsTabChanged: EventEmitter<string | number>;
  hostElement: HTMLStencilElement;
  asLinksChanged(): void;
  fullWidthChanged(): void;
  fullheightChanged(): void;
  navChanged(): void;
  selectedIndexChanged(): void;
  selectedTabnumChanged(): void;
  verticalChanged(): void;
  handleKeyDown(ev: KeyboardEvent): void;
  private handleKeyNext;
  private handleKeyPrevious;
  private handleKeyMoveToContent;
  private handleEnterSpace;
  onTabClick(event: CustomEvent<MdsTabClickedEvent>): void;
  componentWillLoad(): void;
  componentDidLoad(): void;
  selectTabByTabnum(tabnum: string | number, emit?: boolean): void;
  selectTab(index: number, emit?: boolean): void;
  getIndexOfSelectedTab(tabnum: string | number, tabs: HTMLMdsTabsLabelElement[]): number;
  isFocusOnTab(): boolean;
  private keyNext;
  private keyPrevious;
  private keyMoveToContent;
  selectTabAndFocus(index: any): void;
  getContentByIndex(index: number): HTMLDivElement;
  getTabByIndex(index: number): HTMLDivElement;
  focusOnContent(index: number): void;
  private getTabs;
  private getContent;
  private selectTabConditionally;
  private tabSlotChanged;
  render(): JSX.Element;
}
