import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { CopyType } from '@meridian/ui/extras';
export declare class CopyComponent {
  /**
   * The text value to copy to the clipboard when clicked
   */
  copy: string;
  /**
   * Type of animation / display when a value is copied.
   * 'tooltip' shows a tooltip upon copying.
   * 'none' copies without visual feedback.
   * 'overlay' adds an icon in the top right corner of the component and changes to a message when clicked.
   */
  type: CopyType;
  /**
   * The label to display on copy animation with overlay or tooltip
   */
  label: string;
  /**
   * Event emitted when the copy component is clicked
   */
  mdsCopyClick: EventEmitter<void>;
  animating: boolean;
  hostElement: HTMLStencilElement;
  animationId: any;
  private getTooltipElement;
  private copyAndAnimate;
  private onContainerClick;
  copyText(text: string): void;
  render(): JSX.Element;
}
