import { JSX, EventEmitter } from '../../../stencil-public-runtime';
import { HTMLStencilElement } from '../../../stencil-public-runtime';
import { MdsTabClickedEvent } from './tab-clicked-event';
import { InternalRoute } from '@meridian/core';
import { TabsColor } from '@meridian/ui/extras';
export declare class TabsLabelComponent {
  /**
   * A unique tab id must be provided for each `mds-tabs-label`. The same id should be given to the corresponding `mds-tabs-content` element.
   */
  tabnum: string | number;
  /**
   * Destination after clicking the component. This is meant for an external link. Alternatively you can use the routerLink property to navigate to an Angular specific internal route.
   */
  href: string;
  /**
   * Used in conjunction with the href or route attributes. Specifies the target attribute on the anchor tag.
   */
  target: string;
  /**
   * Provide details to route via the framework's router instead of a traditional href on an anchor tag.
   *
   * Use this to keep routing within a single page app instead of letting a whole page load occur.
   */
  route: InternalRoute;
  /**
   * Whether to disable the tab.
   */
  disabled: boolean;
  /**
   * Event emitted when the tab is clicked. Emits the tabnum.
   */
  private_mdsTabClick: EventEmitter<MdsTabClickedEvent>;
  hostElement: HTMLStencilElement;
  aslinks: any;
  parentUUID: any;
  color: TabsColor;
  isActive: boolean;
  vertical: boolean;
  isLastChild: boolean;
  fullwidth: boolean;
  nav: boolean;
  private_setVertical(vertical: boolean): Promise<void>;
  private_setActive(active: boolean): Promise<void>;
  private_setParentUUID(UUID: string): Promise<void>;
  private_setColor(color: TabsColor): Promise<void>;
  private_setAsLinks(aslinks: boolean): Promise<void>;
  private_updateLast(): Promise<void>;
  private_setFullWidth(full: boolean): Promise<void>;
  private_setNav(nav: boolean): Promise<void>;
  componentWillLoad(): void;
  private inheritTabProperties;
  private setIsLastChild;
  private readonly onTabClick;
  private getTabTemplate;
  private getTabItemTemplate;
  render(): JSX.Element;
}
