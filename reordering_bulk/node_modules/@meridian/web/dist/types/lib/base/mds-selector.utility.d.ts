import { SelectorInterface } from './mds-selector.interface';
import { EventEmitter } from '../../stencil-public-runtime';
export declare class MdsSelectorUtility {
  static toggleSelectorChecked(selector: SelectorInterface, selected: boolean, emit: boolean, multiselect: boolean, changeEmitter: EventEmitter<string | boolean>): string | boolean;
  static getRadioCheckedValue(value: unknown, externalFormValue: unknown): boolean;
}
