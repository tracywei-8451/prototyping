import { Host, h, } from '@stencil/core';
export class FlyoutHeaderComponent {
  constructor() {
    this.onActionMenuItemClick = (menuItemLabel) => {
      this.mdsFlyoutMenuActionClick.emit(menuItemLabel);
    };
    this.hideFlyout = () => {
      var _a;
      (_a = this.hostElement.parentElement) === null || _a === void 0 ? void 0 : _a.hide(true);
    };
    this.menuactions = undefined;
    this.header = undefined;
    this.subheader = undefined;
  }
  getMenuActionsTemplate() {
    if (this.menuactions && this.menuactions.length > 0) {
      return (h("mds-dropdown-menu", { buttonsize: "xs" }, this.menuactions.map((action) => (h("mds-dropdown-menu-item", { icon: action.icon, onMdsClick: () => this.onActionMenuItemClick(action.label) }, action.label)))));
    }
    return null;
  }
  render() {
    return (h(Host, { slot: "flyout-header" }, h("header", { class: "mds-flyout-header" }, h("nav", { class: "mds-flyout-header__nav" }, h("mds-button", { label: "Close Flyout", size: "xs", iconsize: "md", type: "supportive", icononly: "close", onClick: () => this.hideFlyout() }), this.getMenuActionsTemplate()), this.header ? (h("mds-heading", { size: "sm", class: "mds-flyout-header__heading" }, this.header)) : null, this.header && this.subheader ? (h("mds-text", { class: "mds-flyout-header__subheading" }, this.subheader)) : null)));
  }
  static get is() { return "mds-flyout-header"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_flyout-header.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_flyout-header.css"]
    };
  }
  static get properties() {
    return {
      "menuactions": {
        "type": "unknown",
        "mutable": false,
        "complexType": {
          "original": "MdsFlyoutMenuAction[]",
          "resolved": "MdsFlyoutMenuAction[]",
          "references": {
            "MdsFlyoutMenuAction": {
              "location": "import",
              "path": "./menu-action"
            }
          }
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The list of actions, labels and for the action menu."
        }
      },
      "header": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "string",
          "resolved": "string",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "Primary header of flyout"
        },
        "attribute": "header",
        "reflect": false
      },
      "subheader": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "string",
          "resolved": "string",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "DEPRECATED: Secondary header of flyout. Use <mds-flyout-header-metadata> instead."
        },
        "attribute": "subheader",
        "reflect": false
      }
    };
  }
  static get events() {
    return [{
        "method": "mdsFlyoutMenuActionClick",
        "name": "mdsFlyoutMenuActionClick",
        "bubbles": false,
        "cancelable": true,
        "composed": true,
        "docs": {
          "tags": [],
          "text": "Event emitted when any of the actions in the menu are clicked. Emits the label of the action that was clicked."
        },
        "complexType": {
          "original": "string",
          "resolved": "string",
          "references": {}
        }
      }];
  }
  static get elementRef() { return "hostElement"; }
}
