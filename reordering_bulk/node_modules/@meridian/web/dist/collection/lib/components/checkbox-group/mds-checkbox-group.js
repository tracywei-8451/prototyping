import { h } from '@stencil/core';
export class CheckboxGroupComponent {
  constructor() {
    this.type = 'stacked';
    this.columnwidth = 0;
  }
  getColumnWidth() {
    return this.columnwidth > 0
      ? { columns: `${this.columnwidth}px auto` }
      : {};
  }
  render() {
    return (h("div", { class: {
        'mds-radio-check-group': true,
        'mds-radio-check-group--inline': this.type === 'inline',
        'mds-radio-check-group--stacked': this.type === 'stacked' && this.columnwidth === 0,
        'mds-radio-check-group--columns': this.columnwidth > 0,
      }, style: this.getColumnWidth() }, h("slot", null)));
  }
  static get is() { return "mds-checkbox-group"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_checkbox-group.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_checkbox-group.css"]
    };
  }
  static get properties() {
    return {
      "type": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "CheckboxGroupLayoutType",
          "resolved": "\"inline\" | \"stacked\"",
          "references": {
            "CheckboxGroupLayoutType": {
              "location": "import",
              "path": "@meridian/ui/extras"
            }
          }
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The type of layout for the checkbox group"
        },
        "attribute": "type",
        "reflect": true,
        "defaultValue": "'stacked'"
      },
      "columnwidth": {
        "type": "number",
        "mutable": false,
        "complexType": {
          "original": "number",
          "resolved": "number",
          "references": {}
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The width of the columns if displayed in a column layout"
        },
        "attribute": "columnwidth",
        "reflect": true,
        "defaultValue": "0"
      }
    };
  }
}
