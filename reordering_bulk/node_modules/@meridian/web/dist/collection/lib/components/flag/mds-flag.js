import { h } from '@stencil/core';
export class FlagComponent {
  constructor() {
    this.color = 'default';
  }
  render() {
    return (h("div", { class: {
        'mds-flag': true,
        'mds-flag--inverse': this.color === 'inverse',
      } }, h("div", { class: "mds-flag__media" }, h("slot", { name: "media" })), h("div", { class: "mds-flag__content" }, h("slot", { name: "content" }))));
  }
  static get is() { return "mds-flag"; }
  static get encapsulation() { return "shadow"; }
  static get originalStyleUrls() {
    return {
      "$": ["_flag.scss"]
    };
  }
  static get styleUrls() {
    return {
      "$": ["_flag.css"]
    };
  }
  static get properties() {
    return {
      "color": {
        "type": "string",
        "mutable": false,
        "complexType": {
          "original": "FlagColor",
          "resolved": "\"default\" | \"inverse\"",
          "references": {
            "FlagColor": {
              "location": "import",
              "path": "@meridian/ui/extras"
            }
          }
        },
        "required": false,
        "optional": false,
        "docs": {
          "tags": [],
          "text": "The color of the flag"
        },
        "attribute": "color",
        "reflect": false,
        "defaultValue": "'default'"
      }
    };
  }
}
