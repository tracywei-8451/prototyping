import { AccordionChevron } from './accordion-chevron.enum';
import { AccordionSize } from './accordion-size.enum';
export interface AccordionStoreProps {
    border?: boolean;
    size?: AccordionSize;
    chevron?: AccordionChevron;
}
export interface AccordionItemStoreProps {
    expanded?: boolean;
}
