export declare const setHostSlot: (component: HTMLElement, slotName: string) => void;
/**
 * This function should only be utilized in complex custom elements that require an additional render cycle.
 * The timeout is used to ensure the element is rendered before the callback is executed.
 * The only current example of this is the ActionBar.svelte which is wrapped by an Angular component with complex logic.
 *
 * @param callback block of code that's needs called in an additional render cycle
 */
export declare const mountCustomElementForExtraCycle: (callback: () => void) => void;
