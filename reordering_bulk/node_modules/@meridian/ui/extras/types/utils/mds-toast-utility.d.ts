import { MdsToastOptions, ToastType } from '../components/Toast/types';
export declare class MdsToastUtility {
    /**
     * This function adds a toast to the toast group. Ensure you have mds-toast-group somewhere on the document.
     *
     * @param message The message of the toast
     * @param type The type of toast
     * @param id A unique ID for the toast
     * @param options Options for the toast.
     * Options include adding a button (action object), hiding toasts after a certain time (autoHide), toggling the click event on a link within the toast (emitLinkClickEvent), and accepting an object to return on click (onActionClick via the action object).
     * The actionLabel prop in Options is deprecated. Use the label prop within the action object instead.
     */
    static addToast: (message: string, type: ToastType, id: string, options?: MdsToastOptions) => void;
    static dismissToast: (id: string) => void;
    static dismissAllToasts: () => void;
}
