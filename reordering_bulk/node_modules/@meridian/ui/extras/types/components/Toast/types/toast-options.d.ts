import { HTMLMdsToastGroupElement } from '../../../index-autogen-html-component-types';
import { MdsToastActionConfig } from './toast-action-config';
export interface MdsToastOptions {
    /**
     * @deprecated DEPRECATED: The label of the action button within a toast. Use the label prop within the action object instead.
     */
    actionLabel?: string;
    mdsToastGroupElement?: HTMLMdsToastGroupElement;
    autoHide?: boolean;
    /**
     * if using links in Toast, toggle off to prevent click event from emitting.
     * Set to true by default
     */
    emitLinkClickEvent?: boolean;
    action?: MdsToastActionConfig;
}
