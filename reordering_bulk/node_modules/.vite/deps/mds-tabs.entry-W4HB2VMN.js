import {
  MdsTabsColorEnum
} from "./chunk-3W3NRPCN.js";
import {
  MdsEventUtility,
  MdsShadowDomUtility,
  MdsStringUtility
} from "./chunk-FRXVAPRB.js";
import {
  Host,
  createEvent,
  getElement,
  h,
  registerInstance
} from "./chunk-XENUAOXZ.js";

// node_modules/@meridian/web/dist/esm/mds-tabs-bcf79e57.js
var tabsCss = 'html{font-size:100%;-webkit-text-size-adjust:100%}*,*::before,*::after{-webkit-box-sizing:inherit;box-sizing:inherit;margin:0;padding:0}article,aside,details,figcaption,figure,footer,header,hgroup,main,nav,section,summary{display:block}[hidden],template{display:none}body{color:var(--mds-color-ink-darker);font-family:var(--mds-font-family-text);line-height:1.5;font-variant-numeric:var(--mds-font-variant-numeric);-webkit-text-size-adjust:100%;-webkit-font-smoothing:antialiased;font-style:normal;font-size:var(--mds-font-size-text-md);font-weight:var(--mds-font-weight-regular);letter-spacing:0;text-align:left;text-decoration:none;text-indent:0;text-shadow:none;min-height:100vh;min-height:-webkit-fill-available}a{color:var(--mds-color-brand-primary);text-decoration:none;outline:0;background:transparent;-webkit-transition:color var(--mds-transition-duration-200);transition:color var(--mds-transition-duration-200)}a:hover,a:focus{color:var(--mds-color-brand-dark)}img,video,object{max-width:100%;height:auto}table{border-collapse:collapse;border-spacing:0;width:100%}th{text-align:left;border:0}h1,h2,h3,h4,h5,h6{margin:0;line-height:1.25}sub,sup{font-size:75%;line-height:0;position:relative;vertical-align:baseline}sup{top:-0.5em}sub{bottom:-0.25em}svg:not(:root){overflow:hidden}p{margin:0;font-size:var(--mds-font-size-text-md)}blockquote{font-size:var(--mds-font-size-text-md);font-style:italic;border-left:1px solid var(--mds-color-edge-light);color:var(--mds-color-ink-dark);padding-left:var(--mds-size-md)}hr{border:0;height:0.125rem;background:var(--mds-color-surface-light);margin:var(--mds-size-lg) 0}abbr{border-bottom:1px dotted var(--mds-color-edge-light);cursor:help}ol,ul{list-style:disc}ol ul,ul ul{list-style-type:circle}ol{list-style-type:decimal}button,input,optgroup,select,textarea{color:inherit;font:inherit;margin:0}button{overflow:visible}button,select{text-transform:none}button[disabled],html input[disabled]{cursor:default}textarea{overflow:auto}strong,b{font-weight:var(--mds-font-weight-semibold)}mark{background-color:var(--mds-color-brand-lighter)}mark::before,mark::after{-webkit-clip-path:inset(100%);clip-path:inset(100%);clip:rect(1px, 1px, 1px, 1px);height:1px;overflow:hidden;position:absolute;white-space:nowrap;width:1px}mark::before{content:" [highlight start] "}mark::after{content:" [highlight end] "}:host{all:initial;display:none;cursor:inherit;line-height:0}:host(.hydrated){display:block}:host([hidden]){display:none}.mds-tabs{color:var(--mds-color-ink-darker);font-family:var(--mds-font-family-text);line-height:1.5;font-variant-numeric:var(--mds-font-variant-numeric);-webkit-text-size-adjust:100%;-webkit-font-smoothing:antialiased;font-style:normal;font-size:var(--mds-font-size-text-md);font-weight:var(--mds-font-weight-regular);letter-spacing:0;text-align:left;text-decoration:none;text-indent:0;text-shadow:none;margin:0;padding:0}.mds-tabs,.mds-tabs:before,.mds-tabs:after,.mds-tabs *,.mds-tabs *:before,.mds-tabs *:after{-webkit-box-sizing:border-box;box-sizing:border-box}.mds-tabs__list{display:-ms-flexbox;display:flex;gap:var(--mds-size-lg);-ms-flex-pack:start;justify-content:flex-start;width:100%;padding:0;border-bottom:1px solid var(--mds-tabs-border-separator-color);margin:0 0 var(--mds-size-sm)}.mds-tabs__list--full-width{display:grid;grid-auto-flow:column;-ms-flex-pack:normal;justify-content:normal}.mds-tabs--full-height{-ms-flex:1;flex:1;display:-ms-flexbox;display:flex;-ms-flex-direction:column;flex-direction:column}:host([vertical]) .mds-tabs{display:-ms-flexbox;display:flex}:host([vertical]) .mds-tabs__list{gap:var(--mds-size-sm);-ms-flex-direction:column;flex-direction:column;width:auto;border-bottom:none;border-left:1px solid var(--mds-tabs-border-separator-color);margin-right:var(--mds-size-lg)}:host([vertical]) .mds-tabs--full-height{-ms-flex-direction:row;flex-direction:row}:host([vertical]) .mds-tabs--full-height .mds-tabs__list{margin-bottom:0}:host([nav]) .mds-tabs{display:-ms-flexbox;display:flex}:host([nav]) .mds-tabs__list{width:auto;-ms-flex-direction:column;flex-direction:column;gap:var(--mds-size-xs);border:none;margin-bottom:0;margin-right:var(--mds-size-lg)}:host([nav]) .mds-tabs--full-height{-ms-flex-direction:row;flex-direction:row}:host([fullheight]:not([fullheight=false])){display:-ms-flexbox;display:flex;height:100%}';
var TabsComponent = class {
  constructor(hostRef) {
    registerInstance(this, hostRef);
    this.mdsTabChanged = createEvent(this, "mdsTabChanged", 3);
    this.aslinks = false;
    this.color = MdsTabsColorEnum.default;
    this.full = false;
    this.fullheight = false;
    this.fullwidth = false;
    this.nav = false;
    this.selectbytabnum = false;
    this.selectedindex = 0;
    this.selectedtabnum = void 0;
    this.vertical = false;
    this.private_UUID = MdsStringUtility.getUUID();
  }
  asLinksChanged() {
    this.getTabs().forEach((item) => {
      item.private_setAsLinks(this.aslinks);
    });
  }
  fullWidthChanged() {
    this.getTabs().forEach((item) => {
      item.private_setFullWidth(this.fullwidth);
    });
  }
  fullheightChanged() {
    this.getContent().forEach((item) => {
      item.private_setFullHeight(this.fullheight);
    });
  }
  navChanged() {
    this.getTabs().forEach((item) => {
      item.private_setNav(this.nav);
    });
  }
  selectedIndexChanged() {
    if (!this.selectbytabnum) {
      this.selectTab(this.selectedindex, false);
    }
  }
  selectedTabnumChanged() {
    if (this.selectbytabnum) {
      this.selectTabByTabnum(this.selectedtabnum, false);
    }
  }
  verticalChanged() {
    this.getTabs().forEach((item) => {
      item.private_setVertical(this.vertical);
    });
  }
  handleKeyDown(ev) {
    if (this.isFocusOnTab()) {
      if (!this.aslinks) {
        this.handleKeyNext(ev);
        this.handleKeyPrevious(ev);
        this.handleKeyMoveToContent(ev);
      }
      this.handleEnterSpace(ev);
    }
  }
  handleKeyNext(ev) {
    if (this.keyNext(ev.key)) {
      const newIndex = this.selectedindex < this.getTabs().length - 1 ? this.selectedindex + 1 : this.selectedindex;
      if (newIndex !== this.selectedindex) {
        const newParent = this.getTabs()[newIndex];
        if (newParent) {
          if (!newParent["disabled"]) {
            this.selectTabAndFocus(newIndex);
          } else if (newParent["disabled"] && newIndex < this.getTabs().length) {
            this.selectTabAndFocus(newIndex + 1);
          }
        }
      }
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleKeyPrevious(ev) {
    if (this.keyPrevious(ev.key)) {
      const newIndex = this.selectedindex > 0 ? this.selectedindex - 1 : this.selectedindex;
      if (newIndex !== this.selectedindex) {
        const newParent = this.getTabs()[newIndex];
        if (newParent) {
          if (!newParent["disabled"]) {
            this.selectTabAndFocus(newIndex);
          } else if (newParent["disabled"] && newIndex > 0) {
            this.selectTabAndFocus(newIndex - 1);
          }
        }
      }
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleKeyMoveToContent(ev) {
    if (this.keyMoveToContent(ev.key)) {
      this.focusOnContent(this.selectedindex);
      MdsEventUtility.cancelEvent(ev);
    }
  }
  handleEnterSpace(ev) {
    if (ev.key === " " || ev.key === "Enter") {
      MdsShadowDomUtility.getActiveElement().click();
      MdsEventUtility.cancelEvent(ev);
    }
  }
  onTabClick(event) {
    if (event.detail.tabParentId === this.private_UUID) {
      this.selectTabByTabnum(event.detail.tabnum);
      MdsEventUtility.cancelEvent(event);
    }
  }
  componentWillLoad() {
    if (!this.hostElement.id) {
      this.hostElement.id = MdsStringUtility.getUUID();
    }
    this.getTabs().forEach((item) => {
      item.private_setParentUUID(this.private_UUID);
      item.private_setAsLinks(this.aslinks);
      item.private_setVertical(this.vertical);
      item.private_setColor(this.color);
      item.private_setFullWidth(this.full || this.fullwidth);
      item.private_setNav(this.nav);
    });
    this.getContent().forEach((item) => {
      item.private_setParentUUID(this.private_UUID);
      item.private_setFullHeight(this.fullheight);
    });
  }
  componentDidLoad() {
    this.selectTabConditionally();
  }
  selectTabByTabnum(tabnum, emit = true) {
    const tabs = this.getTabs();
    if (tabs.length > 0) {
      this.selectTab(this.getIndexOfSelectedTab(tabnum, tabs), emit);
    }
  }
  selectTab(index, emit = true) {
    this.selectedindex = index;
    const tab = this.getTabs()[index];
    if (tab) {
      this.selectedtabnum = tab.tabnum;
    }
    this.getTabs().forEach((item, tabIndex) => {
      item.private_setActive(this.selectedindex === tabIndex);
    });
    this.getContent().forEach((item, contentIndex) => {
      item.private_setActive(this.selectedindex === contentIndex);
    });
    if (emit) {
      this.mdsTabChanged.emit(this.selectbytabnum ? this.getTabs()[this.selectedindex].tabnum : this.selectedindex);
    }
  }
  getIndexOfSelectedTab(tabnum, tabs) {
    let itemIndex = -1;
    tabs.forEach((item, index) => {
      if (item.tabnum == tabnum) {
        itemIndex = index;
      }
    });
    if (itemIndex === -1) {
      return 0;
    }
    return itemIndex;
  }
  isFocusOnTab() {
    return [].slice.call(this.getTabs()).filter((tab) => document.activeElement === tab).length > 0;
  }
  keyNext(key) {
    return !this.vertical && !this.nav && key === "ArrowRight" || (this.vertical || this.nav) && key === "ArrowDown";
  }
  keyPrevious(key) {
    return !this.vertical && !this.nav && key === "ArrowLeft" || (this.vertical || this.nav) && key === "ArrowUp";
  }
  keyMoveToContent(key) {
    return !this.vertical && !this.nav && key === "ArrowDown" || (this.vertical || this.nav) && key === "ArrowRight";
  }
  selectTabAndFocus(index) {
    this.selectTab(index);
    this.getTabByIndex(index).focus();
  }
  getContentByIndex(index) {
    return this.getContent()[index].shadowRoot.querySelector(".mds-tabs-content");
  }
  getTabByIndex(index) {
    return this.getTabs()[index].shadowRoot.querySelector(".mds-tabs-label");
  }
  focusOnContent(index) {
    this.getContentByIndex(index).focus();
  }
  getTabs() {
    return Array.from(this.hostElement.querySelectorAll(`#tabs-${this.private_UUID} > mds-tabs-label`));
  }
  getContent() {
    return Array.from(this.hostElement.querySelectorAll(`#tabs-${this.private_UUID} > mds-tabs-content`));
  }
  selectTabConditionally() {
    if (this.selectbytabnum) {
      this.selectTabByTabnum(this.selectedtabnum, false);
    } else {
      this.selectTab(this.selectedindex, false);
    }
  }
  tabSlotChanged() {
    this.selectTabConditionally();
    this.getTabs().forEach((tab) => tab.private_updateLast());
  }
  render() {
    return h(Host, { id: `tabs-${this.private_UUID}` }, h("div", { class: {
      "mds-tabs": true,
      "mds-tabs--full-height": this.fullheight
    } }, h("div", { class: {
      "mds-tabs__list": true,
      "mds-tabs__list--full-width": this.fullwidth || this.full
    }, role: "tablist", id: `tab-list-${this.private_UUID}` }, h("slot", { name: "tab", onSlotchange: () => this.tabSlotChanged() })), h("slot", { name: "content" })));
  }
  get hostElement() {
    return getElement(this);
  }
  static get watchers() {
    return {
      "aslinks": ["asLinksChanged"],
      "full": ["fullWidthChanged"],
      "fullwidth": ["fullWidthChanged"],
      "fullheight": ["fullheightChanged"],
      "nav": ["navChanged"],
      "selectedindex": ["selectedIndexChanged"],
      "selectedtabnum": ["selectedTabnumChanged"],
      "vertical": ["verticalChanged"]
    };
  }
};
TabsComponent.style = tabsCss;
export {
  TabsComponent as mds_tabs
};
//# sourceMappingURL=mds-tabs.entry-W4HB2VMN.js.map
